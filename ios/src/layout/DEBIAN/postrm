#!/var/jb/bin/sh
# アンインストール後にアイコンキャッシュを更新する
if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
  uicache -u /var/jb/Applications/KoeTomoPlus.app 2>/dev/null || uicache -u /Applications/KoeTomoPlus.app 2>/dev/null || uicache -a 2>/dev/null || true
fi
exit 0
