# Changelog

## [1.01] - 2026-09-04

### 通話（枠）
- **枠を作成できない**不具合を修正。送信先が旧サーバーで、`is_public` / `connection_type` も送っていなかった（公式 TalkRoomApi.createTalkRoom と同じ形式に変更）
- 枠主が退出しても枠が閉じられず残り、次の作成が「ルーム作成超過」で拒否されていた問題を修正（誰も残っていなければ退出時に閉じる）
- 作成超過時は自分の空き枠を自動で閉じて1回だけ再試行。閉じられない場合はその枠に接続
- 枠への参加が公式と異なるサーバー・形式（旧サーバーへJSON）だったのを api2 のフォーム送信に修正。失敗を握り潰していたのも修正
- 枠情報の応答パーサーを1つに統合（data配列 / data.rooms / rooms / talk_rooms / 包みなしトップレベル対応）
- 終了済み（closed_at あり）の枠を参加候補にしていたのを除外
- コメント禁止で作成しても設定が反映されていなかったのを修正
- 枠タイトルを送信前に1〜20文字で検証

### エラー表示
- エラー文言を共通化。ログイン失敗は「メールアドレスかパスワードが違います」、セッション切れ・通信不良・混雑などをそれぞれ日本語で表示。サーバーが理由を返す場合はその文言を使用。判定できないものは「未知のエラーが発生しました（機能名 / status）。管理者に報告してください」

### 大画面・Meta Quest 対応
- 広い画面で本文が右に寄っていたのを、左側UIの実幅を測って画面中央に配置
- 幅に応じて本文列を 900 / 1040 / 1200px に調整、1800px以上は文字を12%拡大
- モーダル・通話画面を大画面向けに拡大、VRブラウザ向けにタップ領域を44〜52pxへ

### 速度・ログ
- 同一の読み取りAPIの重複呼び出しを完了後0.8秒まで1本化（起動時の無駄な通信を削減）
- 「いいね」の送信先の学習結果を保存し、起動ごとの404を解消
- 診断ログ強化：`[ROOM] join/owned/state/create/leave/close`、`[JOIN] POST .../join`、`[SKYWAY] authenticate`

## [1.01] - 2026-09-04

### 修正
- **枠（通話）を作成できない**不具合を修正
  - 送信先が旧サーバーになっていたのを公式と同じ api2 `/api/rooms`（FORM: description のみ、認証はヘッダー）に変更
  - 枠主が退出しても枠が閉じられず残り続け、次回以降サーバーに「ルーム作成超過」で拒否されていた問題を修正（誰も残っていなければ退出時に枠を閉じる）
  - 作成超過になった場合は、残っている自分の空き枠を自動で閉じてから1回だけ作り直す。閉じられない（他の人がいる）場合は、その枠に接続するか、理由を表示する
- 枠作成が失敗したときに、サーバーが返したメッセージをそのまま表示するように（原因が分かるように）
- 「いいね」で旧サーバーを優先する学習結果を保存するように変更。起動のたびに api2 へ 404 になる無駄な通信が1回ずつ発生していた

## [1.00] - 2026-09-03

初回リリース。

### ログイン・アカウント
- メールアドレス／X（Twitter, OAuth2 PKCE）／LINE・Facebook（ID 指定）／トークンのみ、でのログイン
- 複数アカウントの保存と切替（トークンは Android Keystore で暗号化保存）
- 生体認証ロック

### 通話
- 枠の参加・作成、発言リクエスト（挙手）の承認、参加／退出／昇格の通知
- 参加者ごとの音量調整（0〜200%）
- 通話中チャットのリアルタイム受信、新着バブル・未読バッジ、複数行入力
- 役割・コメント・枠名・枠の状態を Firebase Realtime Database で即時反映（発言権の制御は公式と同じ仕組み）
- フローティング通話バブル、PiP、バックグラウンド通話

### タイムライン・投稿
- つぶやく／話そう の閲覧・投稿・いいね・返信、返信の削除
- 投稿の削除、ブックマークの登録・解除・一覧（マイページ「ブックマーク」タブ）
- 左スワイプ→オープン／右スワイプ→フォロー中
- 投稿者・返信者・通知のアイコンをタップでプロフィールへ

### プロフィール・マイページ
- 公式風レイアウト（ヘッダー全幅＋中央にアイコン）、自己紹介の折りたたみ（「詳しく読む」）
- ヘッダー画像・プロフィール画像の変更

### 通知
- 過去の通知のさかのぼり読み込み
- アプリを閉じている間の Android 通知（設定でオン／オフ）
- 通知からサークル・枠・投稿・プロフィールへ直接移動

### 設定・その他
- 見やすく整理した設定画面、プライバシー設定の保持
- DM（テキスト・画像）、コミュニティ（参加／退会／投稿）
- 画像・音声・アイコン／ヘッダーの保存
- 30 種以上のフォント切替
- 自分の投稿が規制対象になったら自動削除して通知（設定でオン／オフ）
- 相手からブロックされている可能性の表示（推定）
- 共有 BAN リスト連携（任意・同意制、1日あたりの上限と急増時の自動停止あり）
- アプリ内アップデート（新バージョン公開時は更新するまで起動不可。リリース説明文に `OPTIONAL_UPDATE` があれば任意更新）

### 業者アカウント対策
- 量産型アイコン・名前のパターン・フォロー0などから業者の可能性を判定し「⚠ 業者?」マークを表示
- 理由はアプリが API から取り直して判定するため偽造できない。共有 BAN リストへの申請ボタン（申請時点ではブロックせず、管理者の承認後に反映）

### 追加の表示
- 投稿に実時刻を表示、削除された投稿を端末の記録から閲覧（マイページ「削除された投稿」）
- プロフィールに最終オンラインの色分け、もらったいいね数、SMS認証・年齢確認・警告回数・コイン／ポイント、API が返す全項目の「詳細情報」
- フォロー一覧をオンライン順に並び替え、マイページに「フォロー中の活動時間帯」グラフ
- 通話中チャットを「アイコン＋名前＋本文」の行表示に。規制対象コメント／ペナルティ中ユーザーのマーク（本文は閲覧可）、コメント禁止枠への対応
- 通話画面に枠の出来事（参加・退出・挙手・昇格・枠名変更など）を表示

### セキュリティ
- 認証トークン・保存アカウントを Android Keystore（AES-256-GCM）で暗号化
- API 暗号化キー・X ログインのクライアント ID を難読化し、署名証明書に結び付け（改変・再署名した APK では使用不可）
- 起動時に署名証明書を検証（改変・再署名・デバッグ可能ビルドでは起動しない）
- 更新の確認・ダウンロード先を GitHub の自リポジトリ配下（https）に限定
- 画像・音声の保存元を https のみに限定、ディープリンク ID を数字のみに制限
- targetSdk 33、`allowBackup=false`、`usesCleartextTraffic=false`

### 軽量化
- APK 約 600 KB。JS/CSS/HTML はビルド時に圧縮
- プロフィールの投稿取得を必要最小限に、通話状況の取得間隔を最適化
