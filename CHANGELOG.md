# Changelog

## [1.07] - 2026-09-09

v1.06 からの変更点です。v1.06 の上に上書きインストールできます（同じ署名）。

### 通話
- ミュートしても相手に声が届くことがあった問題を修正しました。ミュートの ON/OFF を続けて押すと、SkyWay 側の反映が終わる前に次の操作が「もう同じ状態」と判断されて何もせず、ボタンの表示と実際の送信状態が食い違うことがありました。操作を順番に処理し、終わった後に配信の状態が表示と一致するか確かめて、違っていれば掛け直します
- ミュート状態を公式アプリと同じ場所（枠の mute_status）に書くようにしました。これまで公式アプリの参加者からは、こちらのミュートが分かりませんでした。ミュート時は公式アプリのマイクレベル表示を消す合図も送ります
- 公式アプリの参加者がミュートした時のマークを、こちらの参加者一覧にも表示するようにしました
- 「スピーカーOFF」が効いていなかった問題を修正しました。相手の音声は人ごとの音量調整（ゲイン）を通して鳴らしているため、音声要素の消音だけでは止まっていませんでした。全員分のゲインを束ねる親を止めるようにし、スピーカーOFF 中に配信を始めた人も鳴らさないようにしました
- 一度ミュートすると自分の発話表示（緑の枠）とマイクメーターが戻らなかった問題を修正しました（ミュート解除でマイクのトラックが作り直されるのに、音量の解析が古いトラックを見続けていた）
- 枠に入った直後から、先にいる発言者の声が聞こえるようにしました。参加直後は通話画面の名簿が空で、主催者以外の音声は「名簿外」として次の名簿取得（RTDB か数秒〜十数秒後の定期取得）まで購読されていませんでした。参加 API の応答から名簿をすぐ作り、既存の配信を即座に購読します
- 公式の装飾（プロフィール枠・表示バッジ）を通話画面の参加者タイルにも表示します

### 枠一覧・プロフィール
- プロフィールに「いま参加中の枠」を表示します。参加者を見る／その枠に参加、がそこからできます（枠一覧を全部見て、その人がいる枠を探します）
- 枠カードに参加者のアイコン列（主催者→発言者→リスナー、最大 6 人。主催者は金、知り合いは青の縁）を表示します。カードをタップすると入る前に主催者・発言者・リスナーの一覧（名前・アイコン・装飾・知り合いの印）が開き、そこから参加できます。右端の電話マークはすぐ参加です
- 並び替えに「開始が新しい順」「開始が古い順（長く続いている）」を追加しました。開始時刻は公式と同じ `opened_at` を使います（これまで存在しないキーを見ていたため「新しい順」が効いていませんでした）
- 「フォロー中」タブは主催者だけでなく、フォロー中・フォロワー・友達（相互）が参加している枠も表示します。カードに「知り合い ○○」の印を出します

### 業者対策
- 業者アカウントは見つけ次第、自動で共有BANリストに申請します（これまでの「30秒間隔・1時間3件・1日10件」の制限が実態に合わず取りこぼしていたため、8秒間隔・1時間30件・1日150件に。同じ相手は1回だけ。判定と証拠はアプリが公式APIから取り直し、サーバー側でも同じ規則で再検証するので偽造できません。申請だけでブロックはしません）
- 「業者と判定したアカウントの投稿・枠を表示しない」設定を追加しました（初期値オン。フィルター設定で切替。共有BANリスト掲載＋判定「高」が対象）

### 見た目
- 並び替えなどの選択肢が Android 標準の古いダイアログで開いていたのをやめ、アプリの見た目に合わせた下からのシートで選べるようにしました（すべての選択項目に共通）
- プロフィール編集中のプレビューが自分のアイコンにならず頭文字のままだった問題を修正しました。ひとことの表示も 4 行までで折り返すようにしました

### 装飾
- 相手のプロフィールに「もらったギフト」の飾り棚を表示します（公式と同じ `receive_tippings`。相手が非公開なら出ません。タップで贈った人のプロフィール）
- 相手が有効にしている公式「タイムライン背景画像」を、その人の投稿カードの背景に表示します（自分の画像を設定する機能は入っていません）
- 公式アプリの装飾アイテム（プロフィール枠 `decoration_item_id` → `/api/decoration_items` の画像）と表示バッジを、プロフィール・マイページ・タイムライン・枠の参加者一覧・通話画面に表示します。比率は公式の ProfileAvatar と同じ（アイコンは枠画像の 50.7%、バッジは右下 35%）。対応表は 6 時間ごとに取り直します

## [1.06] - 2026-09-09

v1.05 からの変更点です。v1.05 の上に上書きインストールできます（同じ署名）。

### 枠の防御（新機能・設定 → 通話・発話の上）
- コメントの連投（10秒間に N 件以上）・同じ文の繰り返しを検知して、その人のコメントを自分の画面から非表示にするようにしました（件数は設定で変更可）
- 爆音（大音量を出し続ける人）を検知して、その人の音量を自動で下げるようにしました（感度・継続秒数・下げ先の音量は設定で変更可）
- 検知したら「キック」「リスナーに戻す」まで自動で行う設定を追加しました（スパム・爆音それぞれ。初期値は表示・音量の対処のみ）。自分の役割に関係なく要求し、権限が無い場合はサーバーの拒否をそのまま通知します
- 対処のたびに「戻す」ボタン付きの通知を出し、誤検知はその場で取り消せます。判定は枠を出るとリセットされます
- 判定・対処はすべてこの端末の中だけで行い、相手や他の参加者には何も送りません
- 通話画面の上部に盾のマークを追加しました。いま対処している人の一覧（戻す／リスナーに／キック）と、防御の設定をその場で変更できます

### 業者判定・共有BANリスト
- 業者（bot）判定を厳しくしました。これまでは「アイコン・交流・自己紹介・年齢確認」の 4 つ全部が量産型のときだけ判定していたため、アイコンか自己紹介を 1 つ付けるだけで抜けられていました。3 つ＋補強材料（名前の型、既知 bot との ID 連番・同一端末、自己紹介の勧誘・外部誘導の語句）でも判定します。交流が 1〜2 件だけの「ほぼ 0」も同じ扱いです。「単語＋3 桁数字」型の名前は、量産型の特徴が 2 つあれば、また同型の名前が近い ID で複数見つかれば判定します
- 確証のある業者（既知 bot との ID 連番・同一端末・同型名の近接クラスタ）の自動申請は、BAN サーバーの流量制限で弾かれても捨てず、端末内で最長 7 日間再送するようにしました（申請本文に `confidence` と検証用の材料を追加。サーバー側の受け方は docs/banlist-confirmed.md）
- 共有BANリストの「ブロック済み」一覧が長くなってきたため、件数だけの折りたたみ表示にしました。開くと相手の名前がチップで並び（長押し/ホバーで ID）、✕ で解除できます（最大 300 件）
- BAN リストへの申請（自動・手動とも）に通報者と対象の表示名を添えるようにしました（管理画面で ID ではなく名前で確認できます）
- 共有BANリストの自動ブロックが「1 日 40 件・5〜15 秒おき」ではリスト（70 件超）に追いつかなくなっていたため、1 日 200 件・3〜8 秒おきに引き上げました。止まっている理由（本日の上限・急増による一時停止・サーバー制限待ち・画面表示中だけ進む）を進捗の横に表示します。急増による一時停止はその日だけで、翌日は自動で再開します

### 軽量化
- アプリを裏に回した時の通知確認が、Activity 内スレッドと常駐サービスの 2 系統で同じ一覧を二重に取っていたのを 1 系統にしました
- 通知確認はまず未読数だけ（軽い 1 リクエスト）を見て、増えていなければ一覧（ユーザー名の解決を含む重い取得）を取らないようにしました。確認間隔も 12→20 秒から始めて最長 90 秒まで広げます

### 通話（枠主）
- コメント欄の ON/OFF 切替が「パラメータ異常値」で失敗していた問題を修正しました（公式と同じ 1/0 で送信）
- タイトル変更ボタンを送信中は無効にし、結果をトーストで表示するようにしました（反応が無く連打すると同じ変更が何度も送られていた）

## [1.05] - 2026-09-09

v1.04 からの変更点です。v1.04 の上に上書きインストールできます（同じ署名）。


### 速度・安定性
- 旧サーバー（api.meetscom.com）の応答が極端に遅いときに、同じ API を持つ api2 へ自動で切り替えるようにしました。1回の応答が8秒を超えたホストは120秒間避けます
- api2 に存在しない API を覚えて、無駄な問い合わせ（404 → 再送）を繰り返さないようにしました
- 遅い通信1本が他の通信まで待たせていた問題を修正しました（6秒以上かかる通信は同時実行の枠を先に解放）
- 起動時の到着通知を待たずに進むようにしました（旧サーバーの無応答で最大70秒止まることがあった）
- 通信のタイムアウトを見直し、無応答時に別ホストへ早く切り替わるようにしました
- コメント0件の枠に入ったとき、別ホストへ聞き直して待つことがなくなりました
- 通話タブを開き直すたびに枠一覧を全ページ取り直していたのを、90秒以内は差分更新だけにしました

### 投稿
- 投稿の削除が「権限がありません」で失敗する問題を修正しました（削除・投稿・いいね等の書き込みは公式アプリと同じサーバーだけに送るようにしました）
- 投稿の送信で応答が返らなかったとき、投稿が実際に作られていれば「成功」として扱うようにしました（再送による二重投稿も防止）
- 書き込みの待ち時間を延ばし、サーバーが遅いときに投稿が失敗しにくくしました

### チャット
- チャット一覧が空になる・相手の名前が「user 12345」になる問題を修正しました（サーバーの応答形式の変更に対応）
- チャット一覧に未読数のバッジを表示するようにしました
- チャット一覧は前回の内容を先に表示し、最新が届いたら差し替えるようにしました（開いた瞬間の待ち時間をなくしました）

### 表示・その他
- ログイン画面に「このアプリに広告が無い理由・koetomo を応援するには」を追加しました
- チャットが無いときの画面に表示されていた内部情報（応答の生データ）を削除しました


## [1.04] - 2026-09-08

v1.03 からの変更点です。v1.03 の上に上書きインストールできます（同じ署名）。


### 軽量化・発熱対策
- 通話中・待機中の定期処理を一元管理し、画面を消している間は停止または間引き、画面に戻った瞬間に即時更新するようにしました
- 画面のぼかし効果・常時アニメーションを廃止し、描画負荷を大幅に削減しました
- 一覧・通話画面のアイコン画像を表示サイズに縮小してキャッシュするようにしました（デコードとメモリ使用量を削減）
- バックグラウンドの通知確認は、新着が無い間は自動的に間隔を広げるようにしました
- グループ通話の接続方式を公式アプリと同じ SFU に統一しました（人数が多い枠での負荷と通信量を削減）

### 機能
- タイムラインの背景色をカラーピッカーで選べるようにしました（この端末だけの設定。リセットボタン付き）
- 通話画面とマイページに公式オファーウォールの入口を追加しました
- 一覧カードに業者判定のラベルを表示するようにしました（プロフィールを開かずに確認できます）
- ランダム通話の開始前に、相手に表示される自己紹介を確認・変更できるようにしました（通常のプロフィールは変更されません）
- 相手のプロフィールから相互フォローの一覧を開けるようにしました
- 通話中の話者リングの色指定が波紋まで反映されるようにしました

### 不具合修正
- 「タイムライン背景を変更」が端末内の見た目ではなく公開設定を変更していた問題を修正しました（v1.04 では端末内の背景色のみ変更します）
- 投稿に失敗した直後の再試行が「成功」と表示されることがある問題を修正しました
- 投稿直後にマイページを開くと投稿一覧を繰り返し取得し続けることがある問題を修正しました
- 挙手した人の許可・拒否ボタンが表示されないことがある問題を修正しました
- フォロワーのオンライン状態の色が実際と異なることがある問題を修正しました
- ランダム通話でマッチングしない問題を修正しました
- 枠一覧が1件しか表示されない・検索やフォロー中の枠が出ない・スクロールできない問題を修正しました
- 「友達（相互フォロー）」設定項目を削除しました

## [1.03] - 2026-09-05

v1.02 からの変更点です。v1.02 の上に上書きインストールできます（同じ署名）。

### 友達・フォロー
- 「友達」を **相互フォロー** と **友達** の2つに整理し、上のタブで切り替えられるようにしました
- 友達申請に対応：相手のプロフィールから申請、申請中の取り消し、届いた申請の承認／拒否、友達解除ができます
- 「フォロワーから外す」に対応（ブロックせずに相手からのフォローだけを外す）
- フォロー中／フォロワーの一覧を公式に合わせて作り直し。非公開の相手には鍵マーク、相互・フォロー中などの関係を表示します
- フォロー解除ができない不具合を修正しました
- 友達／通知の画面を **左右スワイプ** でタブ切り替えできるようにしました
- 使っていない「自分のQRコード」表示を削除しました

### 通知
- **運営からのお知らせ** タブを追加（タップで内容を開きます）。使っていない「フォロー」タブは削除しました
- アプリを離れていても **通知音と通知が届く** ようにしました
- 通知上部のタブを見やすくしました

### 通話
- 通話中にホームなど他画面へ移動すると、**小窓（最前面）** で通話を続けられるようにしました（設定で表示方法を選べます）
- 通話パネルの「チャット」と「ログ」を1つの画面にまとめました
- 枠の設定に **自動挙手・自動承認・自動拒否** を追加しました

### 投稿・チャット
- 投稿の削除ができない／二重に投稿されてしまう不具合を修正しました
- 投稿はタップすると詳細が開くようにしました
- 会話（チャット）や個別メッセージの削除ができない不具合を修正しました
- チャットを開いたら最新（一番下）が表示されるように、また表示のちらつきを修正しました

### その他
- 投げ銭・通報ができない不具合を修正しました。通報は公式の通報として送られ、共有BANリストは別の機能に分けました
- プロフィールを更新する前のプレビューを追加しました
- 表示・反映の速度を改善しました（投稿してすぐプロフィールに反映されない等を解消し、通信量も削減）
- 公式アプリと同じ通信の作法に合わせる調整を数多く行い、安定性を高めました

### 起動・表示
- 起動時のロード画面を、通話中のマスコットが動く **アニメーション** にしました
- 本文・画像・音声のどれも無い投稿（中身が消えている投稿）は表示しないようにしました
- 本文が空の通話募集投稿には「通話募集の投稿です（本文なし）」と表示するようにしました

### 通知（追加）
- 「新着があります」のポップアップは **初期状態でオフ** にし、設定で表示／非表示を選べるようにしました
- 返信やいいねを見たあとも未読バッジが消えない不具合を修正しました

### 投稿・返信（追加）
- 返信欄の投稿者名が「user 1234567」のように ID 表示になってしまう不具合を修正しました
- いいねした人の取得を見直し、取得できない場合は件数だけでも表示するようにしました
- 返信などが失敗したとき、その理由（相手が削除された・使用できない言葉が含まれる 等）を表示するようにしました
- 長時間バックグラウンドにした後の通信エラーを自動で再試行するようにしました
- 運営からのお知らせの「詳細を開く」が反応しないことがある不具合を修正しました
- 業者判定の「BANリストに申請」ボタンが反応しない／理由の文字が二重にエスケープされる不具合を修正しました
- 名前キャッシュの保存をまとめて行うようにし、スクロール中の無駄なディスク書き込みを減らしました

## [1.02] - 2026-09-04

v1.01 からの変更点です。v1.01 の上に上書きインストールできます（同じ署名）。

### 追加：ランダム通話（マッチング）
- 公式アプリと同じ手順のランダム通話に対応。待ち行列に入り、成立したら相手のプロフィール（アイコン・名前・年齢・地域・自己紹介・もらったいいね）と 61 秒のカウントダウンを表示し、お互いが承諾したときだけ通話が始まります
- 成立の検知はリアルタイム。鮮度の判定（15 秒）、承諾／見送り、相手が見送ったときの扱い、発信側と着信側の振り分けまで公式と同じ規則で実装
- 待ち時間用の小さなゲーム（光ったマスをタップ）を同じ画面に用意。相手が見つかった時点で自動的に止まります

### 追加：グッドトーク
- 通話のあと、公式と同じように相手へ「グッドトーク」（高評価）を送れるようになりました。公式と同じ最短通話秒数の条件を守り、短い通話では表示しません

### 追加：通話（枠）の操作
- 枠のサイズを選べるように（みんなで話す／少人数で話す）。定員と説明は公式の設定をそのまま読み込んで表示します
- 枠主のとき、聞き専の人に「発言をお願い」できるように。相手側には依頼のダイアログが出て、断られたら枠主に通知されます
- 挙手中の人を発言者にする／断る操作を追加
- 「誰がどこにいるか」の一覧がリアルタイム情報と食い違ってズレていたのを修正
- グループ通話の一覧に「もっと見る」を追加

### 追加：業者アカウント対策
- 必須条件（16 文字ランダムのアイコン名・フォロー／フォロワー／友達／いいねがすべて 0・自己紹介なし・年齢確認なし）をすべて満たし、加点（名前が「単語＋3 桁数字」・既知の業者と ID が連番・同一の feature・直近 1 時間に 5 件以上投稿など）が基準を超えた相手だけを、共有 BAN リストへ自動で申請します
- 判定と証拠はアプリが API から取り直して作るため偽造できません。**申請のみで、ブロックはしません**（ブロックは管理者の承認後）
- 暴走防止：同じ相手は 1 回まで、30 秒間隔、1 時間 3 件／1 日 10 件まで、起動直後は動作しない
- 設定 →「フィルター」でオフにできます

### 追加：DM・チャット
- DM の画像が表示されない不具合を修正（非公開ストレージの署名付き URL をアプリ側で生成）
- DM で音声を送れるように
- 画像の拡大表示を Google マップと同じ操作感に（つまむ位置を中心にズーム、ダブルタップ、上限 8 倍、範囲外に出ない）
- 送信した内容がすぐ画面に出るように（送信中の表示を挟み、失敗したら元に戻す）
- 少しスクロールしただけでチャットが閉じてしまう問題を修正

### 追加：投稿・保存
- 音声投稿を開いても再生できなかった不具合を修正（投稿の詳細に再生プレイヤーと保存ボタンを表示）
- 投稿の詳細から画像をタップして拡大できるように
- 音声の保存形式に MP3（320kbps・高音質）を追加（元の形式／MP3 128kbps／MP3 320kbps／WAV）
- 画像の保存形式を選べるように（元の形式／JPEG／PNG／WebP）。元の形式のままなら PNG・WebP・GIF などを判別して正しい拡張子で保存します（これまでは中身に関わらず `.jpg` で保存していました）
- 保存ボタンを押したときに形式をきく画面へ「**次回から聞かずにこの形式で保存する**」を追加。**音声と画像で別々**に設定でき、確認なしにしたあとでも保存ボタンの**長押し**でその場で選べます
- 保存先フォルダを3つから選べるように。おすすめ（既定）は従来どおりの KoeTomo フォルダ（画像は 画像/KoeTomo、音声は 音楽/KoeTomo）。ほかに「ダウンロード内の KoeTomo 専用フォルダ（音声と画像を自動で振り分け）」「ダウンロード直下」
- インストール後の初回に一度だけ、保存先を選ぶ案内を表示します（マイページ →「保存(ダウンロード)」でいつでも変更可）
- 形式をきく画面が拡大表示や投稿詳細の**裏に隠れて**、それらを閉じたあとにタイムライン上へ出てきていたのを修正

### 追加：その他の機能
- 運営アンケートへの回答、自分の QR コード、録音チャンネルの開始／停止、通知登録など、未対応だった公式 API に対応
- フォント選択を iOS 風の一覧に刷新し、47 種類に拡張（標準・かわいい・ドット）
- 保存データ／キャッシュの画面を設定に追加。使用量の表示と削除ボタン、何が消えて何が残るかの説明つき

### 変更：プライバシー
- 「フォロー中の活動時間帯」の集計を、ログイン時に**集計するかどうか選べる**ように変更。初期状態はオフで、あとから設定でいつでも切り替えられます（保存するのは時間帯ごとの件数だけで、どこにも送信しません）

### 修正：通知
- 保存完了もお知らせも通話中も、通知アイコンがすべて受話器（📞）だったのをやめ、アプリ自身のロゴを使うように変更
- お知らせ通知が鳴らない端末があったのを修正（音量設定を変更できない古い通知チャンネルが残っていたため、チャンネルを作り直して古いものを削除）
- 通知の見出しが全部「声とも+ 通知」で区別できなかったのを、**相手の名前を見出し・内容を本文**に変更。長い本文も省略されず全部読めるように
- 保存完了の通知とメッセージに**保存先フォルダ**とファイル名を表示
- DM の受信をリアルタイムで受け取れるように
- 未読の確認間隔を短縮し、アプリを開いた瞬間にも確認するように

### 修正：フォント
- 選んだフォントがボタンや入力欄に反映されず、端末の既定フォントのままになっていたのを修正
- 一覧からフォントを選んでも、以前に読み込んだフォントファイルが起動のたびに上書きしてしまう不具合を修正
- 英数字しか持たないフォント（Press Start 2P・Silkscreen・Pixelify Sans・Micro 5）で日本語が化けないように、日本語のフォールバックを追加し、一覧に「英数字だけ」と表示

### 修正：タップした行と違う項目が反応する
- 通話ログの一覧で、通話が終わるたびに記録が1件増えると、タップした行の**1つ下（1つ古い通話）**の詳細が開いていたのを修正
- 参加履歴から通話ログを開くとき、同じ人の枠に短時間で複数回入っていると隣の通話が開くことがあったのを修正
- 音声の連続再生中の自動スクロールで、指の下のボタンが動いて別の行が反応することがあったのを修正
- 通知一覧で、種類を絞り込んでいると1ページ目で「これ以上ありません」になり続きが読めなかったのを修正
- タイムラインの応答に投稿一覧が含まれないとき、画面が真っ白のまま何も出なかったのを修正

### 速度
- 起動を大幅に短縮。保存済みアカウントがあれば認証の通信を待たずに画面を表示し、データ取得だけを裏で待たせます（表示まで約 1.3 秒 → 約 0.2 秒）
- 起動直後の空白をなくすスプラッシュを追加。暗号化ストアの初期化を裏で先に済ませ、フォントの読み込みが描画を止めないように変更
- タイムラインとチャットの画面外の行を描画しないように（スクロールが軽くなります）
- 未読確認の間隔を、操作していないときは自動でゆっくりに（電池と通信の節約）

### 容量
- 未使用の CSS を全数検査して削除（194KB → 120KB。全画面 1,717 要素の計算スタイルを比較し、見た目が変わらないことを確認）
- 端末に置く一時ファイルの上限を設けて自動整理。設定からキャッシュ削除もできます
- APK を zopfli で再圧縮（中身は同一）

### 整理
- 設定の「データ・履歴」から、専用の画面がすでにある項目（友達の投稿・録音一覧・ポイント履歴・サブスク・所有コミュニティ・友達・バッジ・マッチング・受け取った投げ銭）を削除

### 制限の記録
- 投稿や枠作成がサーバー側の制限で弾かれたとき、時刻・前回成功からの秒数・サーバーの文言を端末内に記録する画面を追加（公式アプリにもアプリ内のクールタイムは無く、制限はサーバー判定のみであることを確認済み）

## [1.01] - 2026-09-03

### 修正
- 枠（通話）を作成できない不具合を修正（送信先が旧サーバー、`is_public` / `connection_type` 未送信）
- 枠主の退出時に枠が閉じられず残る問題を修正
- 枠への参加のサーバー・形式を公式と同じ api2 のフォーム送信に修正
- 終了済みの枠を参加候補から除外、コメント禁止設定が反映されない問題を修正
- エラー文言の共通化、大画面対応、起動時の重複通信の削減

## [1.00] - 2026-09-03

初回リリース。
